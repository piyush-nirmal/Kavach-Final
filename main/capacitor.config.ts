import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kavach.health',
  appName: 'Kavach Health Companion',
  webDir: 'dist'
};

export default config;
